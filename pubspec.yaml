name: ycoo_forum
description: "源论坛(YcoForum)民间移动端 - 阅读侧。"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.13.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.2.0
  html: ^0.15.4
  shared_preferences: ^2.3.3
  cronet_http: ^1.9.0
  webview_flutter: ^4.10.0
  file_picker: ^10.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
